 npm i react-icons framer-motion react-router-dom 
  @emailjs/browser

  @layer base   ==basic style body
@layer components == reusable components (btn)
@layer utilities == choti helper classes (text shadow)

linear-gradient(direction, color1, color2, color3...)